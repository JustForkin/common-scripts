#!/bin/bash
pkgver=$(wget -cqO- https://www.linuxmint.com/ | grep "?p=" | grep "div" | head -n 1 | cut -d '"' -f 4 | cut -d '=' -f 2)

if [[ $pkgver == 3590 ]]; then
    printf "Mint\n"
else
    printf "Mint${pkgver}\n"
fi
