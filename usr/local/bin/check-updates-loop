#!/bin/bash
while :
do
    timestamp=$(date +"%r %A, %d %B %Y")
    printf "Running /usr/local/bin/check-updates at $timestamp\n"
    /usr/local/bin/check-updates
    if [[ -n "$@" ]]; then
         sleep "$@"
    else
         sleep 10m
    fi
done
